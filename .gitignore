# Dependencies. The 2011 vendored set that used to live in node_modules/ is
# archived in git history at commit 047c23d; see CODEBASE.md and CHARTER A6.
node_modules/
new_modules/
# Root-anchored: the legacy v2 lockfile stays untracked, but v3/package-lock.json is
# tracked deliberately — as of cycle 4 v3 has a real runtime dependency (pg) and the
# lockfile is what makes its transitive set reproducible.
/package-lock.json
package.json.bak

# v3 build artefacts
v3/node_modules/
v3/dist/
*.tsbuildinfo
